---

version: '3.3'

services:
  db:
    image: postgres:12.2
    environment:
      - POSTGRES_DB=test-db
  test:
    build:
      context: .
      dockerfile: Dockerfile.test
    command: make test-t
    environment:
      - CLJ_ENV=test
    env_file:
      - './.env.test'
    depends_on:
      - db

volumes:
  pgdata:
